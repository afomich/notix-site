---
// Английская главная живёт в корне: её URL проиндексирован и меняться не должен.
// Остальные локали генерирует [locale]/index.astro из того же компонента.
import HomePage from '../components/HomePage.astro';
import { DEFAULT_LOCALE } from '../i18n';
---

<HomePage locale={DEFAULT_LOCALE} />
