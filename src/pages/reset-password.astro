---
// Desktop fallback for the password-reset universal link. On a phone with Notix
// installed the OS routes https://notix-ai.app/reset-password?token=... straight
// into the app and this page is never seen; a desktop mail client lands here.
// The page must never read or transmit the token — static copy only, noindex.
import Base from '../layouts/Base.astro';
import { DEFAULT_LOCALE, dictionary } from '../i18n';

const locale = DEFAULT_LOCALE;
const t = dictionary(locale).resetPassword;
---

<Base
  locale={locale}
  title={t.seoTitle}
  description={t.seoDescription}
  noindex
  pageClass="page-flex"
>
  <main class="wrap" style="text-align:center; padding-top: 96px; padding-bottom: 96px;">
    <h1>{t.title}</h1>
    <p style="max-width: 44ch; margin: 16px auto 0;">
      {t.text}
    </p>
    <p style="max-width: 44ch; margin: 12px auto 0;">
      {t.noAppText}
    </p>
    <div style="margin-top: 28px;">
      <a class="btn-primary" href="https://apps.apple.com/app/id6758561284">{t.cta}</a>
    </div>
  </main>
</Base>
